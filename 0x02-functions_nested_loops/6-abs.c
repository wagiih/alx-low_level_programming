#include "main.h"

/**
 * int _abs - computes the absolute value of an integer.
 * @n: the int to check
 * Return: absolute value of int
 */
int _abs(int)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (i * -1)
	}
}
